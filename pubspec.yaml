name: petcare_app
description: A PetCare Management Mobile App
publish_to: 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.1
  firebase_auth: ^4.7.1
  cloud_firestore: ^4.9.1
  firebase_storage: ^11.2.3
  image_picker: ^1.0.4
  flutter_local_notifications: ^17.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
